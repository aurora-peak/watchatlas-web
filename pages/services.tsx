import ComingSoon from "@/components/ComingSoon";

export default function ServicesPage() {
  return (
    <ComingSoon
      title="Browse by service"
      blurb="A directory of every streaming service we track, with title and region coverage counts — your entry point into per-service catalogs."
    />
  );
}
